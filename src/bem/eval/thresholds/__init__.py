"""bem.eval.thresholds — Stage E4: threshold tuning on the dev split.

Modules
-------
tuner   : pure threshold-tuning algorithms (no I/O)
runner  : orchestrator — loads data, calls tuner, writes outputs + manifest
"""
