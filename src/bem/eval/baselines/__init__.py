# bem.eval.baselines — classical baseline scoring modules
#
# Fairness constraints enforced throughout:
#   AND: Author(s) ID never used in any fair baseline.
#   AIN: title / year / source never used.
